#include<stdio.h>
void main(){
	float length=5.16,width=3.16;
	float perimeter=2*(length+width);
	printf("The perimeter of the rectangle with length %f cm and width %f cm is %f",length,width,perimeter);
}